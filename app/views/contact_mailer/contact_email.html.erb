<!DOCTYPE html>
<hml>
    <head>
    </head>
    <body>
         <p>You have recieved a message from the sites contact form, from <%= "#{name}, #{email}." %></p> 
         <p><%= @body %></p>
    </body>
</hml>